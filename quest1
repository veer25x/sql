q-1: Create a table Employee with id, name, salary, department.

drop table employee;

create table employee (
  id int primary key,
  name varchar(50),
  salary int,
department varchar(50)
);

select*from employee;
